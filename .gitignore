# ── Dependencies and runtime ─────────────────────────────
node_modules/
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ── Playwright MCP ─────────────────────────────
.playwright-mcp/

# ── WhatsApp session (with login credentials)────────────────
wa-bot/.wwebjs_auth/
wa-bot/.wwebjs_cache/

# ──Business data (generated at runtime, not included in version control)─────────
data/excel/*.xlsx
data/receipts/
data/uploads/
data/images/
data/pending_receipts.json
data/sessions.json
data/*.db*
data/backup/
data/admin_sessions/
wa-bot/data/

# ── Log ─────────────────────────────────────
logs/*.log

# ── Test coverage products ────────────────────────────
wa-bot/coverage/

# ── System/Environment ─────────────────────────────
.DS_Store
*.env

# ── CI/CD temporary files ─────────────────────────────
.gemini/
gha-creds-*.json

# ── Git worktrees ──────────────────────────────
.worktrees/

# ── IDE / Editor ─────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ── Root directory development files (for local testing only)───────────────
/package.json
/package-lock.json
!wa-bot/package.json
!wa-bot/package-lock.json

.codebuddy
.workbuddy
node_modules.mac/

# ── Local Agent / Generated product ────────────────────────
.agents/
skills-lock.json
docs/html/

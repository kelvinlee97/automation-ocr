# ── 依赖和运行时 ──────────────────────────────
node_modules/
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ── Playwright MCP ─────────────────────────────
.playwright-mcp/

# ── WhatsApp session（含登录凭证）────────────────
wa-bot/.wwebjs_auth/
wa-bot/.wwebjs_cache/

# ── 业务数据（运行时产生，不入版本控制）─────────
data/excel/*.xlsx
data/receipts/
data/uploads/
data/images/
data/pending_receipts.json
data/sessions.json

# ── 日志 ──────────────────────────────────────
logs/*.log

# ── 测试覆盖率产物 ────────────────────────────
wa-bot/coverage/

# ── 系统 / 环境 ────────────────────────────────
.DS_Store
*.env

# ── CI/CD 临时文件 ─────────────────────────────
.gemini/
gha-creds-*.json

# ── Git worktrees ──────────────────────────────
.worktrees/

# ── IDE / 编辑器 ───────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ── 根目录开发文件（仅本地测试用）───────────────
/package.json
/package-lock.json
!wa-bot/package.json
!wa-bot/package-lock.json

.codebuddy
.workbuddy
node_modules.mac/

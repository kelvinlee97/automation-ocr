# 依赖和运行时文件
node_modules/
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.playwright-mcp
.serena/

# WhatsApp session 数据（含登录凭证，不入版本控制）
wa-bot/.wwebjs_auth/
wa-bot/.wwebjs_cache/

# 业务数据文件（按需备份，不入版本控制）
data/excel/*.xlsx
data/uploads/
data/images/
data/pending_receipts.json
data/sessions.json

# 日志文件
logs/*.log

# 系统文件
.DS_Store
*.env

.gemini/
gha-creds-*.json

# Git worktrees（隔离开发目录，不入版本控制）
.worktrees/

# IDE 和工具
.idea/
.vscode/
*.swp
*.swo

# 本地测试文件
.playwright-mcp/
opencode.json

# 根目录 package.json（仅供本地测试）
package-lock.json
root package.json
!wa-bot/package.json
